{
  "name": "pidantuan-bot",
  "version": "1.0.0",
  "description": "Telegram bot using Playwright to login and claim award",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "playwright": "^1.47.0",
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.64.0"
  }
}
